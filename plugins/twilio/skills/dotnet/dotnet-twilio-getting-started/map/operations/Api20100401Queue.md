<!-- Generated file — do not edit; regenerated with the SDK. -->

# Api20100401Queue — operations

Accessor: `client.Api20100401Queue` · Source: `Api/Api20100401Queue.cs` · 5 operations

**Type sources**: the file declaring each type an operation names (`RawError` excluded — see sdk-map.md).

### CreateQueue

- **Signature**: `CreateQueue(string accountSid, string friendlyName, int? maxSize, RequestOptions? requestOptions = null, CancellationToken ct = default)`
  - `maxSize` — nullable, no default → **must pass explicitly**
- **Returns**: `ApiV2010AccountQueue`
- **Error**: `SdkException<RawError>` — **Case B**

| Type | Source |
| --- | --- |
| `ApiV2010AccountQueue` | `Models/ApiV2010AccountQueue.cs` |

### DeleteQueue

- **Signature**: `DeleteQueue(string accountSid, string sid, RequestOptions? requestOptions = null, CancellationToken ct = default)`
- **Returns**: `void` (Task)
- **Error**: `SdkException<RawError>` — **Case B**

### FetchQueue

- **Signature**: `FetchQueue(string accountSid, string sid, RequestOptions? requestOptions = null, CancellationToken ct = default)`
- **Returns**: `ApiV2010AccountQueue`
- **Error**: `SdkException<RawError>` — **Case B**

| Type | Source |
| --- | --- |
| `ApiV2010AccountQueue` | `Models/ApiV2010AccountQueue.cs` |

### ListQueue

- **Signature**: `ListQueue(string accountSid, long? pageSize, int? page, string? pageToken, RequestOptions? requestOptions = null, CancellationToken ct = default)`
  - `pageSize` — nullable, no default → **must pass explicitly**
  - `page` — nullable, no default → **must pass explicitly**
  - `pageToken` — nullable, no default → **must pass explicitly**
- **Query params (wire ← C#)**: `PageSize` ← `pageSize`, `Page` ← `page`, `PageToken` ← `pageToken`
- **Returns**: `ListQueueResponse`
- **Error**: `SdkException<RawError>` — **Case B**

| Type | Source |
| --- | --- |
| `ListQueueResponse` | `Models/ListQueueResponse.cs` |

### UpdateQueue

- **Signature**: `UpdateQueue(string accountSid, string sid, string? friendlyName, int? maxSize, RequestOptions? requestOptions = null, CancellationToken ct = default)`
  - `friendlyName` — nullable, no default → **must pass explicitly**
  - `maxSize` — nullable, no default → **must pass explicitly**
- **Returns**: `ApiV2010AccountQueue`
- **Error**: `SdkException<RawError>` — **Case B**

| Type | Source |
| --- | --- |
| `ApiV2010AccountQueue` | `Models/ApiV2010AccountQueue.cs` |


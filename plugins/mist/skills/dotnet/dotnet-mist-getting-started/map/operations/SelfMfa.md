# SelfMfa — operations

Accessor: `client.SelfMfa` · Source: `Api/SelfMfa.cs` · 2 operations

**Parameter names are literal.** Signatures are generated code verbatim — in named arguments use the exact parameter names shown (the cancellation-token parameter is named `ct`).

### GenerateSecretFor2FaVerification
- **HTTP**: `GET /api/v1/self/two_factor/token` (ApiHost (api))
- **Notes**: Generate Secret Key for 2FA verification
- **Signature**: `GenerateSecretFor2FaVerification(MfaSecretType? by, RequestOptions? requestOptions = null, CancellationToken ct = default)`
  - `by` — nullable, no default → **must pass explicitly**
  - defaults: `requestOptions` = null
- **Query params (wire ← C#)**: `by` ← `by`
- **Returns**: `ResponseTwoFactorJson`
- **Error**: `SdkException<GenerateSecretFor2FaVerificationError>` — **Case A (typed)**
- **Error accessors**: `TryGetResponseHttp400(out ResponseHttp400)` [400, 401, 403, 429] · `TryGetResponseHttp404(out ResponseHttp404)` [404] · `TryGetRawError(out RawError)` [fallback]
- **No-throw variant**: absent
- **Pagination**: none

### VerifyTwoFactor
- **HTTP**: `POST /api/v1/self/two_factor/verify` (ApiHost (api))
- **Notes**: Verify Two-factor (OTP) To verify two-factor authentication by using a code generated by app (e.g. Google Authenticator, Authy). Upon successful verification, the `two_factor_passed` will be set to true if it hasn’t already been.
- **Signature**: `VerifyTwoFactor(TwoFactorCode? body, RequestOptions? requestOptions = null, CancellationToken ct = default)`
  - `body` — nullable, no default → **must pass explicitly**
  - defaults: `requestOptions` = null
- **Returns**: `void` (Task)
- **Error**: `SdkException<VerifyTwoFactorError>` — **Case A (typed)**
- **Error accessors**: `TryGetResponseHttp400(out ResponseHttp400)` [400, 401, 403, 429] · `TryGetResponseHttp404(out ResponseHttp404)` [404] · `TryGetRawError(out RawError)` [fallback]
- **No-throw variant**: absent
- **Pagination**: none

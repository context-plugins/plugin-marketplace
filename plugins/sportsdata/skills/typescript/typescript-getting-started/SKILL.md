---
name: "typescript-getting-started"
description: "SportsDataIO TypeScript SDK identity and lookup layer (TypeScript/JavaScript only) — install, the single import specifier `sports-data-io`, the server environments and the base-URL knob, the auth pattern, the SDK map that ships inside the installed package (`sdk-map.md` + `map/operations/`) and how to traverse it, and the file table naming the one source file owning each fact the map leaves to the source. Load this before answering any SportsDataIO TypeScript SDK contract question or writing any SDK code."
---

# Getting started with the SportsDataIO TypeScript SDK

> **Who this skill is for.** This is the **lookup layer** for anyone writing SportsDataIO TypeScript SDK code — it is yours to follow directly and fully. Ground every contract fact here (in the SDK map, and in the source files it names) rather than in recall, and carry those facts onto a contract sheet before you implement. Load `typescript-integrate-sportsdata` for the workflow that wraps this skill.

This is the **SDK-specific** entry point. For general patterns that apply to any APIMatic-generated TypeScript SDK (client construction, auth, calling endpoints, models, error handling, resilience, testing), see the companion API-agnostic skills: `typescript-client-initialization`, `typescript-authentication`, `typescript-calling-endpoints`, `typescript-models`, `typescript-error-handling`, `typescript-configuration-resilience` and `typescript-testing`.

**This page and those companion skills are complementary — load both.** This page is authoritative for the SDK's *identity and surface* (what to install, what to import, the resources, which file owns which fact); the companion skills are the *usage layer* on top — the best-practice way to call each piece and the gotchas a signature cannot show. Reading a file in the installed package does not remove the need to load the skill for that step, so at each step below, load the companion *and* confirm names against the installed package.

## SDK identity

Verified against `package.json` and `sdk-map.md` of the generated package at version `1.0`. **Re-verify after a version bump** — this page is a snapshot, not a live read.

| Fact | Value |
| --- | --- |
| API | SportsDataIO |
| Package name (what you install, and what you import) | `sports-data-io` — **not on npm**; built from source (see *Install*) |
| Import specifier | `sports-data-io` — the package root is the **only** entry; deep imports do not resolve |
| Version | `1.0` (API spec version `1.0`) |
| Client class | `SportsDataIoClient` (`src/client.ts`) — one class, no sync/async split |
| Options type | `ClientOptions`, with `DEFAULT_CLIENT_OPTIONS` beside it (`src/client-options.ts`) |
| Client construction | `new SportsDataIoClient(clientOptions: Partial<ClientOptions> = {})` — **every** field is optional, so `new SportsDataIoClient()` compiles. Fields: `serverEnvironment` · `serverOptions` · `timeout` · `fetch` · `apiKeyHeader` · `apiKeyQuery`. `timeout` defaults to `60_000` ms |
| Auth | **API key (header)** — set `ClientOptions.apiKeyHeader`<br>**API key (query)** — set `ClientOptions.apiKeyQuery` |
| Environments | 1 environment (`ServerEnvironment.Production` *(default)*) × 1 server group |
| Base-URL config | `serverOptions.<group>.<environment>.baseUrl` (`src/servers.ts`), defaulting to `https://api.sportsdata.io` |
| Node floor | `>=20` (`engines.node`) |
| Runtime dependency | `zod` (`^3.25.0 \|\| ^4.0.0`), imported as `zod/v4-mini` — the only one |
| Module format | dual ESM + CommonJS folder dialects (`dist/esm`, `dist/commonjs`) behind one export |
| Typing | the package ships its own `.d.ts` and is generated under strict TypeScript. Callers get full inference — **a type error against this SDK is a real contract violation, not noise** |
| Surface | 741 operations · 50 resources · 406 models · 5 open enums · 741 per-operation error subclasses |

The table above is **orientation, not a copy-paste recipe** — it gives you the names and facts (install, import specifier, the auth *pattern*, the base-URL knob), while the actual integration code comes from the companion skills. Load each one as you reach its step (see **Integration workflow** below) and confirm its types against the installed package.

## Install

This SDK is not published to npm, so the install comes straight from the repository the plugin records for it:

```bash
npm install git+https://github.com/context-plugins/sportsdata-typescript-sdk#main
```

That fetches everything the package's `files` list packs — `src/`, `sdk-map.md` and the pages under `map/operations/` — so **every lookup on this page works as soon as the install finishes**. **Make sure the installed package is built**, as the source on GitHub is not pre-built.

Do not vendor its `src/` into your project, point `tsconfig` `paths` at a throwaway clone, or import from `dist/` directly. Installing the package properly is what makes the `exports` map, the shipped `.d.ts` chain and the dual-dialect resolution behave the way the SDK expects — **and it is what puts the SDK map inside `node_modules`, which is where every lookup below reads it from**. Requires Node `>=20` (`engines.node`).

## Imports — one entry, and only one

**Every** public name is re-exported from the package root — the client, `ClientOptions`, `ServerEnvironment`, 411 model types with the schema value beside each, the error classes, and the runtime types (`ApiPromise`, `ApiResult`, `RequestOptions`, `ErrorPayload`, `Declared`, `Schema`, `EnumSchema`, `Encoded`).

```ts
import { SportsDataIoClient, ServerEnvironment, ResponseError, SportsDataIoError } from "sports-data-io";
import type { ClientOptions, Area } from "sports-data-io";
```

Things the specifier alone will not tell you:

- **Deep imports do not resolve.** The `exports` map exposes `.` and `./package.json` and nothing else, so `sports-data-io/models/…` fails (`TS2307`) even though the file exists in the shipped `src/`. Every `Source` path on the SDK map is where to **read** a shape, never what to import.
- **⚠ The SDK exports a model type literally named `Event`** (`src/models/event.ts`, schema `eventSchema`). Import it unaliased and it **shadows the global `Event`** for the rest of the file. Alias it: `import type { Event as SdkEvent } from "sports-data-io"`.
- **From CommonJS**, the typed spelling is `import sdk = require("sports-data-io")`. A plain `require` destructure runs but yields `any`.
- **`instanceof` is reliable within one dialect.** A process that loads both (`import` in one file, `require` in another) gets two independent copies of every error class, and `instanceof` across that boundary is `false` — narrow on `err.kind` / `err.payload.kind` / `err.name` there.

Under `verbatimModuleSyntax`, names carrying no runtime value (`ClientOptions`, every model type) must be imported with `import type`. Under `exactOptionalPropertyTypes`, **omit or spread** an absent optional field rather than assigning `undefined` to it.

## Environments

`ClientOptions.serverEnvironment` selects one environment for the whole client (`src/servers.ts`). `ServerEnvironment` is a `const` object with a derived union type — not a TypeScript `enum` — and unlike the model enums it is **closed**, so only its declared members are assignable.

| Group | Environment | Base URL | Override at |
| --- | --- | --- | --- |
| `default` | `production` *(default)* | `https://api.sportsdata.io` | `serverOptions.default.production.baseUrl` |

Consequences to state on every contract sheet that touches configuration:

- Constructing the client with no options selects **`ServerEnvironment.Production`**, silently.
- ⚠ **This SDK declares exactly one environment**, so reaching any other host is a `serverOptions` override, not an environment value — `serverOptions: { default: { production: { baseUrl: "…" } } }`. Note the key is still `production`: the environment name and the host it points at are now decoupled, which is exactly the shape of configuration that gets a live secret pointed at a test host or the reverse. Make the deployment's intent explicit where the client is built, and verify it.
- An override merges with the built-in default **per group-and-environment pair, key by key**; a `baseUrl` override replaces the template verbatim, template variable values are percent-encoded into it, and templates expand per request rather than once at construction.
- Each operation is bound to one server group at generation time. A map block carries a **Server** bullet only when its group is not `default`.
- An environment value the SDK does not know throws `SdkError` **synchronously out of the operation method** at the first call — not at construction — so `try`/`await` catches it but `.asApiResult()` and `.catch()` never see it.

## Auth pattern (2 schemes)

Authentication is **per operation**: every operation declares the requirement it enforces and the SDK sends exactly that. Each block on a map page carries an **Auth** bullet, `none` included. There is no client-global switch and no per-call override. 741 of the 741 operations require a credential and 0 are public.

| `ClientOptions` field | Scheme kind | What the SDK sends |
| --- | --- | --- |
| `apiKeyHeader` | API key (header) | header `Ocp-Apim-Subscription-Key: <key>` |
| `apiKeyQuery` | API key (query) | query `key=<key>` |

```ts
const client = new SportsDataIoClient({
  apiKeyHeader: process.env.API_KEY!,
  apiKeyQuery: process.env.API_KEY!,
});
```

**Every credential field is optional at the type level and that is a trap worth flagging on every sheet.** Omit one and nothing fails at construction — the request simply goes out without that credential and the server decides. Most APIs then answer `401`; one that serves anonymous traffic answers `200` and hides the omission entirely. So a `401` on a call you believed was authenticated is usually an unset field rather than an SDK failure; verify the field is set rather than waiting for a `401` to tell you, and check the operation's **Auth** bullet against what the client was actually given.

Three more behaviours the type does not show:

- **A credential may be a function.** Every field typed `TokenProvider` is re-read on **every** request with no caching, so a key can rotate without rebuilding the client. An empty string counts as absent; a function counts as present without being invoked.
- **Composition is emitted, not configured.** Where the spec puts two schemes in one requirement the SDK sends **both**; where it lists alternatives it sends the **first configured** one, in the order the **Auth** bullet prints them.
- **A 401 invalidates, it does not retry.** On a 401 (401 only, not 403) the SDK clears whatever that operation's scheme had cached, so the *next* call re-acquires; the current request still rejects.

See `typescript-authentication` for the full picture.

## Resources

Resources are **memoized lazy getters** on the client (`client.<attr>`). Their classes are exported only for their merged namespaces — the per-operation request and error types — and for `instanceof`; their constructors take engine internals that are not exported, so reach a resource only through its getter.

| Attribute | Class | Ops | Operations |
| --- | --- | --- | --- |
| `client.nflV3Scores` | `NflV3Scores` | 44 | `nflV3ScoresAreGamesInProgress` · `nflV3ScoresByeWeeks` · `nflV3ScoresDepthChartsAll` · `nflV3ScoresDepthChartsByActive` · `nflV3ScoresDepthChartsByWeek` · `nflV3ScoresGamesByDateFinal` · `nflV3ScoresGamesByDateLiveFinal` · `nflV3ScoresGamesBySeasonLiveFinal` · `nflV3ScoresGamesByWeekFinal` · `nflV3ScoresGamesByWeekLiveFinal` · `nflV3ScoresGamesBasicByWeekFinal` · `nflV3ScoresGamesBasicByWeekLiveFinal` · `nflV3ScoresNews` · `nflV3ScoresNewsByDate` · `nflV3ScoresNewsByTeam` · `nflV3ScoresPlayerDetailsAll` · `nflV3ScoresPlayerDetailsByFreeAgents` · `nflV3ScoresPlayerDetailsByRookieDraftYear` · `nflV3ScoresPlayerDetailsByTeam` · `nflV3ScoresPlayerGameLogsBySeason` · `nflV3ScoresPlayerProfilesAll` · `nflV3ScoresPlayerProfilesByFreeAgent` · `nflV3ScoresPlayerProfilesByRookieDraftYear` · `nflV3ScoresPlayerProfilesByTeam` · `nflV3ScoresReferees` · `nflV3ScoresSchedules` · `nflV3ScoresSchedulesBasic` · `nflV3ScoresScoresByWeekSimulation` · `nflV3ScoresSeasonCurrent` · `nflV3ScoresSeasonLastCompleted` · `nflV3ScoresSeasonUpcoming` · `nflV3ScoresStadiums` · `nflV3ScoresStandings` · `nflV3ScoresTeamGameStatsLiveFinal` · `nflV3ScoresTeamProfilesAll` · `nflV3ScoresTeamProfilesByActive` · `nflV3ScoresTeamProfilesBySeason` · `nflV3ScoresTeamProfilesBasicAll` · `nflV3ScoresTeamSeasonStats` · `nflV3ScoresTimeframes` · `nflV3ScoresTransactionsByDate` · `nflV3ScoresWeekCurrent` · `nflV3ScoresWeekLastCompleted` · `nflV3ScoresWeekUpcoming` |
| `client.nflV3Projections` | `NflV3Projections` | 13 | `nflV3ProjectionsDfsSlateOwnershipProjectionsBySlate` · `nflV3ProjectionsDfsSlateOwnershipProjectionsUpcoming` · `nflV3ProjectionsDfsSlatesByDate` · `nflV3ProjectionsDfsSlatesByWeek` · `nflV3ProjectionsIdpProjectedPlayerGameStatsByTeam` · `nflV3ProjectionsIdpProjectedPlayerGameStatsByWeek` · `nflV3ProjectionsPlayerDetailsByInjured` · `nflV3ProjectionsProjectedFantasyDefenseGameStatsWithDfsSalaries` · `nflV3ProjectionsProjectedFantasyDefenseSeasonStatsWithAdp` · `nflV3ProjectionsProjectedPlayerGameStatsByTeam` · `nflV3ProjectionsProjectedPlayerGameStatsByWeek` · `nflV3ProjectionsProjectedPlayerSeasonStatsWithAdp` · `nflV3ProjectionsProjectedPlayerSeasonStatsWithAdpByTeam` |
| `client.nflV3RotoBallerPremiumNews` | `NflV3RotoBallerPremiumNews` | 3 | `nflV3NewsRotoballerPremiumNews` · `nflV3NewsRotoballerPremiumNewsByDate` · `nflV3NewsRotoballerPremiumNewsByTeam` |
| `client.nflV3Odds` | `NflV3Odds` | 34 | `nflV3OddsBettingEventsByDate` · `nflV3OddsBettingEventsBySeason` · `nflV3OddsBettingFuturesBySeason` · `nflV3OddsBettingFuturesBySeasonSportsbookGroup` · `nflV3OddsBettingMarket` · `nflV3OddsBettingMarketSportsbookGroup` · `nflV3OddsBettingMarketsByEvent` · `nflV3OddsBettingMarketsByEventSportsbookGroup` · `nflV3OddsBettingMarketsByGame` · `nflV3OddsBettingMarketsByGameSportsbookGroup` · `nflV3OddsBettingMarketsByMarketType` · `nflV3OddsBettingMarketsByMarketTypeSportsbookGroup` · `nflV3OddsBettingMetadata` · `nflV3OddsBettingPlayerPropsByGame` · `nflV3OddsBettingPlayerPropsByGameSportsbookGroup` · `nflV3OddsBettingResultsByMarket` · `nflV3OddsBettingResultsByMarketSportsbookGroup` · `nflV3OddsBettingSplitsByBettingMarket` · `nflV3OddsBettingSplitsByGame` · `nflV3OddsBettingTrendsByMatchup` · `nflV3OddsBettingTrendsByTeam` · `nflV3OddsInGameOddsByWeek` · `nflV3OddsInGameOddsByWeekSportsbookGroup` · `nflV3OddsInGameOddsLineMovement` · `nflV3OddsInGameOddsLineMovementSportsbookGroup` · `nflV3OddsInGameOddsLineMovementWithResultingSportsbookGroup` · `nflV3OddsPeriodGameOddsByWeek` · `nflV3OddsPeriodGameOddsLineMovement` · `nflV3OddsPreGameAndPeriodGameOddsByWeekSportsbookGroup` · `nflV3OddsPreGameAndPeriodGameOddsLineMovementSportsbookGroup` · `nflV3OddsPreGameAndPeriodGameOddsLineMovementWithResultingSportsbookGroup` · `nflV3OddsPreGameOddsByWeek` · `nflV3OddsPreGameOddsLineMovement` · `nflV3OddsSportsbooksActive` |
| `client.nflV3Stats` | `NflV3Stats` | 34 | `nflV3StatsBoxScoreByTeamFinal` · `nflV3StatsBoxScoreByTeamLiveFinal` · `nflV3StatsBoxScoreFinal` · `nflV3StatsBoxScoreLiveFinal` · `nflV3StatsBoxScoresByWeekFinal` · `nflV3StatsBoxScoresDeltaByWeek` · `nflV3StatsBoxScoresSimulation` · `nflV3StatsFantasyDefenseGameStatsAll` · `nflV3StatsFantasyDefenseGameStatsByTeam` · `nflV3StatsFantasyDefenseSeasonStatsAll` · `nflV3StatsFantasyDefenseSeasonStatsByTeam` · `nflV3StatsFantasyPlayerOwnershipPercentagesSeasonLongByWeek` · `nflV3StatsFantasyPointsByWeek` · `nflV3StatsGamesBySeasonFinal` · `nflV3StatsInjuriesAll` · `nflV3StatsInjuriesByTeam` · `nflV3StatsPlayerGameLogsBySeason` · `nflV3StatsPlayerGameRedZoneStats` · `nflV3StatsPlayerGameRedZoneStatsInsideFive` · `nflV3StatsPlayerGameRedZoneStatsInsideTen` · `nflV3StatsPlayerGameStatsByTeamFinal` · `nflV3StatsPlayerGameStatsByTeamLiveFinal` · `nflV3StatsPlayerGameStatsByWeekFinal` · `nflV3StatsPlayerGameStatsByWeekLiveFinal` · `nflV3StatsPlayerGameStatsDelta` · `nflV3StatsPlayerGameStatsDeltaByWeek` · `nflV3StatsPlayerSeasonRedZoneStats` · `nflV3StatsPlayerSeasonRedZoneStatsInsideFive` · `nflV3StatsPlayerSeasonRedZoneStatsInsideTen` · `nflV3StatsPlayerSeasonStats` · `nflV3StatsPlayerSeasonStatsByTeam` · `nflV3StatsPlayerSeasonThirdDownStats` · `nflV3StatsProBowlers` · `nflV3StatsTeamGameStatsByGameFinal` |
| `client.nflV3PlayByPlay` | `NflV3PlayByPlay` | 6 | `nflV3PbpPlayByPlayByTeamFinal` · `nflV3PbpPlayByPlayByTeamLiveFinal` · `nflV3PbpPlayByPlayFinal` · `nflV3PbpPlayByPlayLiveFinal` · `nflV3PbpPlayByPlayDelta` · `nflV3PbpPlayByPlaySimulation` |
| `client.nflV3Headshots` | `NflV3Headshots` | 1 | `nflV3HeadshotsHeadshots` |
| `client.nbaV3Scores` | `NbaV3Scores` | 27 | `nbaV3ScoresAreGamesInProgress` · `nbaV3ScoresDepthCharts` · `nbaV3ScoresGamesByDateFinal` · `nbaV3ScoresGamesByDateLiveFinal` · `nbaV3ScoresGamesBasicByDateFinal` · `nbaV3ScoresGamesBasicByDateLiveFinal` · `nbaV3ScoresNews` · `nbaV3ScoresNewsByDate` · `nbaV3ScoresPlayerDetailsByActive` · `nbaV3ScoresPlayerDetailsByFreeAgent` · `nbaV3ScoresPlayerDetailsByTeam` · `nbaV3ScoresPlayerProfilesByActive` · `nbaV3ScoresPlayerProfilesByFreeAgent` · `nbaV3ScoresPlayersProfilesByTeam` · `nbaV3ScoresReferees` · `nbaV3ScoresSchedules` · `nbaV3ScoresSchedulesBasic` · `nbaV3ScoresSeasonCurrent` · `nbaV3ScoresStadiums` · `nbaV3ScoresStandings` · `nbaV3ScoresTeamGameLogsBySeason` · `nbaV3ScoresTeamGameStatsByDateLiveFinal` · `nbaV3ScoresTeamProfilesAll` · `nbaV3ScoresTeamProfilesByActive` · `nbaV3ScoresTeamProfilesBySeason` · `nbaV3ScoresTeamSeasonStats` · `nbaV3ScoresTransactions` |
| `client.nbaV3Projections` | `NbaV3Projections` | 6 | `nbaV3ProjectionsDfsSlatesByDate` · `nbaV3ProjectionsPlayerDetailsByInjured` · `nbaV3ProjectionsProjectedPlayerGameStatsByDate` · `nbaV3ProjectionsProjectedPlayerSeasonStats` · `nbaV3ProjectionsProjectedPlayerSeasonStatsByTeam` · `nbaV3ProjectionsStartingLineupsByDate` |
| `client.nbaV3RotoBallerPremiumNews` | `NbaV3RotoBallerPremiumNews` | 2 | `nbaV3NewsRotoballerPremiumNews` · `nbaV3NewsRotoballerPremiumNewsByDate` |
| `client.nbaV3Odds` | `NbaV3Odds` | 34 | `nbaV3OddsBettingEventsByDate` · `nbaV3OddsBettingEventsBySeason` · `nbaV3OddsBettingFuturesBySeason` · `nbaV3OddsBettingFuturesBySeasonSportsbookGroup` · `nbaV3OddsBettingMarket` · `nbaV3OddsBettingMarketSportsbookGroup` · `nbaV3OddsBettingMarketsByEvent` · `nbaV3OddsBettingMarketsByEventSportsbookGroup` · `nbaV3OddsBettingMarketsByGame` · `nbaV3OddsBettingMarketsByGameSportsbookGroup` · `nbaV3OddsBettingMarketsByMarketType` · `nbaV3OddsBettingMarketsByMarketTypeSportsbookGroup` · `nbaV3OddsBettingMetadata` · `nbaV3OddsBettingPlayerPropsByGame` · `nbaV3OddsBettingPlayerPropsByGameSportsbookGroup` · `nbaV3OddsBettingResultsByMarket` · `nbaV3OddsBettingResultsByMarketSportsbookGroup` · `nbaV3OddsBettingSplitsByBettingMarket` · `nbaV3OddsBettingSplitsByGame` · `nbaV3OddsBettingTrendsByMatchup` · `nbaV3OddsBettingTrendsByTeam` · `nbaV3OddsInGameOddsByDate` · `nbaV3OddsInGameOddsByDateSportsbookGroup` · `nbaV3OddsInGameOddsLineMovement` · `nbaV3OddsInGameOddsLineMovementSportsbookGroup` · `nbaV3OddsInGameOddsLineMovementWithResultingSportsbookGroup` · `nbaV3OddsPeriodGameOddsByDate` · `nbaV3OddsPeriodGameOddsLineMovement` · `nbaV3OddsPreGameAndPeriodGameOddsByDateSportsbookGroup` · `nbaV3OddsPreGameAndPeriodGameOddsLineMovementSportsbookGroup` · `nbaV3OddsPreGameAndPeriodGameOddsLineMovementWithResultingSportsbookGroup` · `nbaV3OddsPreGameOddsByDate` · `nbaV3OddsPreGameOddsLineMovement` · `nbaV3OddsSportsbooksByActive` |
| `client.nbaV3Stats` | `NbaV3Stats` | 14 | `nbaV3StatsAllStars` · `nbaV3StatsBoxScoreFinal` · `nbaV3StatsBoxScoreLiveFinal` · `nbaV3StatsBoxScoresByDateFinal` · `nbaV3StatsBoxScoresByDateLiveFinal` · `nbaV3StatsBoxScoresDeltaByDate` · `nbaV3StatsFantasyPointsByDate` · `nbaV3StatsPlayerGameLogsBySeason` · `nbaV3StatsPlayerGameStatsByDateFinal` · `nbaV3StatsPlayerGameStatsByDateLiveFinal` · `nbaV3StatsPlayerSeasonStats` · `nbaV3StatsPlayerSeasonStatsByTeam` · `nbaV3StatsTeamGameStatsByDateFinal` · `nbaV3StatsTeamStatsAllowedByPosition` |
| `client.nbaV3PlayByPlay` | `NbaV3PlayByPlay` | 3 | `nbaV3PbpPlayByPlayFinal` · `nbaV3PbpPlayByPlayLiveFinal` · `nbaV3PbpPlayByPlayDelta` |
| `client.nbaV3Headshots` | `NbaV3Headshots` | 1 | `nbaV3HeadshotsHeadshots` |
| `client.mlbV3Scores` | `MlbV3Scores` | 26 | `mlbV3ScoresAreGamesInProgress` · `mlbV3ScoresGamesByDateFinal` · `mlbV3ScoresGamesByDateLiveFinal` · `mlbV3ScoresGamesBasicByDateFinal` · `mlbV3ScoresGamesBasicByDateLiveFinal` · `mlbV3ScoresNews` · `mlbV3ScoresNewsByDate` · `mlbV3ScoresPlayerDetailsByActive` · `mlbV3ScoresPlayerDetailsByFreeAgents` · `mlbV3ScoresPlayerProfilesByActive` · `mlbV3ScoresPlayerProfilesByFreeAgents` · `mlbV3ScoresPlayerProfilesByTeam` · `mlbV3ScoresPlayersDetailsByTeam` · `mlbV3ScoresSchedules` · `mlbV3ScoresSchedulesBasic` · `mlbV3ScoresSeasonCurrent` · `mlbV3ScoresStadiums` · `mlbV3ScoresStandings` · `mlbV3ScoresTeamGameLogsBySeason` · `mlbV3ScoresTeamGameStatsByDateFinal` · `mlbV3ScoresTeamGameStatsByDateLiveFinal` · `mlbV3ScoresTeamProfilesAll` · `mlbV3ScoresTeamProfilesByActive` · `mlbV3ScoresTeamProfilesBySeason` · `mlbV3ScoresTeamSeasonStats` · `mlbV3ScoresTransactions` |
| `client.mlbV3Projections` | `MlbV3Projections` | 8 | `mlbV3ProjectionsDepthCharts` · `mlbV3ProjectionsDfsSlateOwnershipProjectionsBySlate` · `mlbV3ProjectionsDfsSlateOwnershipProjectionsUpcoming` · `mlbV3ProjectionsDfsSlatesByDate` · `mlbV3ProjectionsPlayerDetailsByInjured` · `mlbV3ProjectionsProjectedPlayerGameStatsByDate` · `mlbV3ProjectionsProjectedPlayerSeasonStatsWithAdp` · `mlbV3ProjectionsStartingLineupsByDate` |
| `client.mlbV3RotoBallerPremiumNews` | `MlbV3RotoBallerPremiumNews` | 2 | `mlbV3NewsRotoballerPremiumNews` · `mlbV3NewsRotoballerPremiumNewsByDate` |
| `client.mlbV3Odds` | `MlbV3Odds` | 34 | `mlbV3OddsBettingEventsByDate` · `mlbV3OddsBettingEventsBySeason` · `mlbV3OddsBettingFuturesBySeason` · `mlbV3OddsBettingFuturesBySeasonSportsbookGroup` · `mlbV3OddsBettingMarket` · `mlbV3OddsBettingMarketSportsbookGroup` · `mlbV3OddsBettingMarketsByEvent` · `mlbV3OddsBettingMarketsByEventSportsbookGroup` · `mlbV3OddsBettingMarketsByGame` · `mlbV3OddsBettingMarketsByGameSportsbookGroup` · `mlbV3OddsBettingMarketsByMarketType` · `mlbV3OddsBettingMarketsByMarketTypeSportsbookGroup` · `mlbV3OddsBettingMetadata` · `mlbV3OddsBettingPlayerPropsByGame` · `mlbV3OddsBettingPlayerPropsByGameSportsbookGroup` · `mlbV3OddsBettingResultsByMarket` · `mlbV3OddsBettingResultsByMarketSportsbookGroup` · `mlbV3OddsBettingSplitsByGame` · `mlbV3OddsBettingSplitsByBettingMarket` · `mlbV3OddsBettingTrendsByMatchup` · `mlbV3OddsBettingTrendsByTeam` · `mlbV3OddsInGameOddsByDate` · `mlbV3OddsInGameOddsByDateSportsbookGroup` · `mlbV3OddsInGameOddsLineMovement` · `mlbV3OddsInGameOddsLineMovementSportsbookGroup` · `mlbV3OddsInGameOddsLineMovementWithResultingSportsbookGroup` · `mlbV3OddsPeriodGameOddsByDate` · `mlbV3OddsPeriodGameOddsLineMovement` · `mlbV3OddsPreGameAndPeriodGameOddsByDateSportsbookGroup` · `mlbV3OddsPreGameAndPeriodGameOddsLineMovementSportsbookGroup` · `mlbV3OddsPreGameAndPeriodGameOddsLineMovementWithResultingSportsbookGroup` · `mlbV3OddsPreGameOddsByDate` · `mlbV3OddsPreGameOddsLineMovement` · `mlbV3OddsSportsbooksByActive` |
| `client.mlbV3Stats` | `MlbV3Stats` | 17 | `mlbV3StatsBatterVsPitcherStats` · `mlbV3StatsBoxScoreFinal` · `mlbV3StatsBoxScoreLiveFinal` · `mlbV3StatsBoxScoresByDateFinal` · `mlbV3StatsBoxScoresByDateLiveFinal` · `mlbV3StatsBoxScoresDeltaByDate` · `mlbV3StatsFantasyPointsByDate` · `mlbV3StatsPlayerGameLogsBySeason` · `mlbV3StatsPlayerGameStatsByDateFinal` · `mlbV3StatsPlayerGameStatsByDateLiveFinal` · `mlbV3StatsPlayerSeasonSplitStats` · `mlbV3StatsPlayerSeasonStats` · `mlbV3StatsPlayerSeasonStatsByAway` · `mlbV3StatsPlayerSeasonStatsByHome` · `mlbV3StatsPlayerSeasonStatsByTeam` · `mlbV3StatsPlayerSeasonStatsSplitByTeam` · `mlbV3StatsTeamHittingVsStartingPitcher` |
| `client.mlbV3PlayByPlay` | `MlbV3PlayByPlay` | 3 | `mlbV3PbpPlayByPlayFinal` · `mlbV3PbpPlayByPlayLiveFinal` · `mlbV3PbpPlayByPlayDelta` |
| `client.mlbV3Headshots` | `MlbV3Headshots` | 1 | `mlbV3HeadshotsHeadshots` |
| `client.nhlV3Scores` | `NhlV3Scores` | 27 | `nhlV3ScoresAreGamesInProgress` · `nhlV3ScoresDepthChartsGoalies` · `nhlV3ScoresGamesByDateFinal` · `nhlV3ScoresGamesByDateLiveFinal` · `nhlV3ScoresGamesBasicByDateFinal` · `nhlV3ScoresGamesBasicByDateLiveFinal` · `nhlV3ScoresNews` · `nhlV3ScoresNewsByDate` · `nhlV3ScoresPlayerDetailsByActive` · `nhlV3ScoresPlayerDetailsByActive2` · `nhlV3ScoresPlayerDetailsByFreeAgents` · `nhlV3ScoresPlayerDetailsByFreeAgents2` · `nhlV3ScoresPlayersDetailsByTeam` · `nhlV3ScoresPlayersProfilesByTeam` · `nhlV3ScoresReferees` · `nhlV3ScoresSchedules` · `nhlV3ScoresSchedulesBasic` · `nhlV3ScoresSeasonCurrent` · `nhlV3ScoresStadiums` · `nhlV3ScoresStandings` · `nhlV3ScoresTeamGameLogsBySeason` · `nhlV3ScoresTeamGameStatsByDateLiveFinal` · `nhlV3ScoresTeamProfilesAll` · `nhlV3ScoresTeamProfilesByActive` · `nhlV3ScoresTeamProfilesBySeason` · `nhlV3ScoresTeamSeasonStats` · `nhlV3ScoresTransactions` |
| `client.nhlV3Projections` | `NhlV3Projections` | 4 | `nhlV3ProjectionsDfsSlatesByDate` · `nhlV3ProjectionsPlayerDetailsByInjured` · `nhlV3ProjectionsProjectedPlayerGameStatsByDate` · `nhlV3ProjectionsStartingGoaltendersByDate` |
| `client.nhlV3Odds` | `NhlV3Odds` | 34 | `nhlV3OddsBettingEventsByDate` · `nhlV3OddsBettingEventsBySeason` · `nhlV3OddsBettingFuturesBySeason` · `nhlV3OddsBettingFuturesBySeasonSportsbookGroup` · `nhlV3OddsBettingMarket` · `nhlV3OddsBettingMarketSportsbookGroup` · `nhlV3OddsBettingMarketsByEvent` · `nhlV3OddsBettingMarketsByEventSportsbookGroup` · `nhlV3OddsBettingMarketsByGame` · `nhlV3OddsBettingMarketsByGameSportsbookGroup` · `nhlV3OddsBettingMarketsByMarketType` · `nhlV3OddsBettingMarketsByMarketTypeSportsbookGroup` · `nhlV3OddsBettingMetadata` · `nhlV3OddsBettingPlayerPropsByGame` · `nhlV3OddsBettingPlayerPropsByGameSportsbookGroup` · `nhlV3OddsBettingResultsByMarket` · `nhlV3OddsBettingResultsByMarketSportsbookGroup` · `nhlV3OddsBettingSplitsByBettingMarket` · `nhlV3OddsBettingSplitsByGame` · `nhlV3OddsBettingTrendsByMatchup` · `nhlV3OddsBettingTrendsByTeam` · `nhlV3OddsInGameOddsByDate` · `nhlV3OddsInGameOddsByDateSportsbookGroup` · `nhlV3OddsInGameOddsLineMovement` · `nhlV3OddsInGameOddsLineMovementSportsbookGroup` · `nhlV3OddsInGameOddsLineMovementWithResultingSportsbookGroup` · `nhlV3OddsPeriodGameOddsByDate` · `nhlV3OddsPeriodGameOddsLineMovement` · `nhlV3OddsPreGameAndPeriodGameOddsByDateSportsbookGroup` · `nhlV3OddsPreGameAndPeriodGameOddsLineMovementSportsbookGroup` · `nhlV3OddsPreGameAndPeriodGameOddsLineMovementWithResultingSportsbookGroup` · `nhlV3OddsPreGameOddsByDate` · `nhlV3OddsPreGameOddsLineMovement` · `nhlV3OddsSportsbooksByActive` |
| `client.nhlV3Stats` | `NhlV3Stats` | 14 | `nhlV3StatsBoxScoreFinal` · `nhlV3StatsBoxScoreLiveFinal` · `nhlV3StatsBoxScoresByDateFinal` · `nhlV3StatsBoxScoresByDateLiveFinal` · `nhlV3StatsBoxScoresDeltaByDate` · `nhlV3StatsFantasyPointsByDate` · `nhlV3StatsLineCombinationsBySeason` · `nhlV3StatsPlayerGameLogsBySeason` · `nhlV3StatsPlayerGameStatsByDateFinal` · `nhlV3StatsPlayerGameStatsByDateLiveFinal` · `nhlV3StatsPlayerSeasonStats` · `nhlV3StatsPlayerSeasonStatsByTeam` · `nhlV3StatsTeamGameStatsByDateFinal` · `nhlV3StatsTeamStatsAllowedByPosition` |
| `client.nhlV3PlayByPlay` | `NhlV3PlayByPlay` | 3 | `nhlV3PbpPlayByPlayFinal` · `nhlV3PbpPlayByPlayLiveFinal` · `nhlV3PbpPlayByPlayDelta` |
| `client.nhlV3Headshots` | `NhlV3Headshots` | 1 | `nhlV3HeadshotsHeadshots` |
| `client.nhlV3RotoBallerPremiumNews` | `NhlV3RotoBallerPremiumNews` | 2 | `nhlV3NewsRotoballerPremiumNews` · `nhlV3NewsRotoballerPremiumNewsByDate` |
| `client.cfbV3Scores` | `CfbV3Scores` | 26 | `cfbV3ScoresAreGamesInProgress` · `cfbV3ScoresConferenceHierarchy` · `cfbV3ScoresGamesByDateLiveFinal` · `cfbV3ScoresGamesByWeekFinal` · `cfbV3ScoresGamesByWeekLiveFinal` · `cfbV3ScoresGamesBasicByDateLiveFinal` · `cfbV3ScoresGamesBasicByWeekFinal` · `cfbV3ScoresGamesBasicByDateFinal` · `cfbV3ScoresGamesByDateFinal` · `cfbV3ScoresPlayerDetailsByActive` · `cfbV3ScoresPlayerDetailsByActive2` · `cfbV3ScoresPlayerDetailsByInjured` · `cfbV3ScoresPlayerDetailsByTeam` · `cfbV3ScoresPlayerProfilesByTeam` · `cfbV3ScoresSchedules` · `cfbV3ScoresSchedulesBasic` · `cfbV3ScoresSeasonCurrent` · `cfbV3ScoresSeasonCurrentDetails` · `cfbV3ScoresSeasonTypeCurrent` · `cfbV3ScoresStadiums` · `cfbV3ScoresTeamGameLogsBySeason` · `cfbV3ScoresTeamGameStatsByWeekLiveFinal` · `cfbV3ScoresTeamProfilesAll` · `cfbV3ScoresTeamSeasonStatsStandings` · `cfbV3ScoresTeamsBasicAll` · `cfbV3ScoresWeekCurrent` |
| `client.cfbV3Odds` | `CfbV3Odds` | 34 | `cfbV3OddsBettingEventsByDate` · `cfbV3OddsBettingEventsBySeason` · `cfbV3OddsBettingFuturesBySeason` · `cfbV3OddsBettingFuturesBySeasonSportsbookGroup` · `cfbV3OddsBettingMarket` · `cfbV3OddsBettingMarketSportsbookGroup` · `cfbV3OddsBettingMarketsByEvent` · `cfbV3OddsBettingMarketsByEventSportsbookGroup` · `cfbV3OddsBettingMarketsByGame` · `cfbV3OddsBettingMarketsByGameSportsbookGroup` · `cfbV3OddsBettingMarketsByMarketType` · `cfbV3OddsBettingMarketsByMarketTypeSportsbookGroup` · `cfbV3OddsBettingMetadata` · `cfbV3OddsBettingPlayerPropsByGame` · `cfbV3OddsBettingPlayerPropsByGameSportsbookGroup` · `cfbV3OddsBettingResultsByMarket` · `cfbV3OddsBettingResultsByMarketSportsbookGroup` · `cfbV3OddsBettingSplitsByBettingMarket` · `cfbV3OddsBettingSplitsByGame` · `cfbV3OddsBettingTrendsByMatchup` · `cfbV3OddsBettingTrendsByTeam` · `cfbV3OddsInGameOddsByWeek` · `cfbV3OddsInGameOddsByWeekSportsbookGroup` · `cfbV3OddsInGameOddsLineMovement` · `cfbV3OddsInGameOddsLineMovementSportsbookGroup` · `cfbV3OddsInGameOddsLineMovementWithResultingSportsbookGroup` · `cfbV3OddsPeriodGameOddsByWeek` · `cfbV3OddsPeriodGameOddsLineMovement` · `cfbV3OddsPreGameAndPeriodGameOddsByWeekSportsbookGroup` · `cfbV3OddsPreGameAndPeriodGameOddsLineMovementSportsbookGroup` · `cfbV3OddsPreGameAndPeriodGameOddsLineMovementWithResultingSportsbookGroup` · `cfbV3OddsPreGameOddsByWeek` · `cfbV3OddsPreGameOddsLineMovement` · `cfbV3OddsSportsbooksByActive` |
| `client.cfbV3Stats` | `CfbV3Stats` | 13 | `cfbV3StatsBoxScoreFinal` · `cfbV3StatsBoxScoreLiveFinal` · `cfbV3StatsBoxScoresByDateFinal` · `cfbV3StatsBoxScoresByDateLiveFinal` · `cfbV3StatsBoxScoresByWeekFinal` · `cfbV3StatsBoxScoresByWeekLiveFinal` · `cfbV3StatsBoxScoresDeltaByWeek` · `cfbV3StatsPlayerGameLogsBySeason` · `cfbV3StatsPlayerGameStatsByWeekFinal` · `cfbV3StatsPlayerGameStatsByWeekLiveFinal` · `cfbV3StatsPlayerSeasonStats` · `cfbV3StatsPlayerSeasonStatsByTeam` · `cfbV3StatsTeamGameStatsByWeekFinal` |
| `client.cbbV3Scores` | `CbbV3Scores` | 22 | `cbbV3ScoresAreGamesInProgress` · `cbbV3ScoresGamesByDateFinal` · `cbbV3ScoresGamesByDateLiveFinal` · `cbbV3ScoresGamesBySeason` · `cbbV3ScoresGamesBySeasonFinal` · `cbbV3ScoresGamesBasicByDate` · `cbbV3ScoresLeagueHierarchy` · `cbbV3ScoresPlayerDetailsByActive` · `cbbV3ScoresPlayerDetailsByActive2` · `cbbV3ScoresPlayerDetailsByInjured` · `cbbV3ScoresPlayerDetailsByTeam` · `cbbV3ScoresPlayerProfilesByTeam` · `cbbV3ScoresSchedulesByTeam` · `cbbV3ScoresSchedulesBasic` · `cbbV3ScoresSeasonCurrent` · `cbbV3ScoresStadiums` · `cbbV3ScoresTeamGameLogsBySeason` · `cbbV3ScoresTeamGameStatsByDateLiveFinal` · `cbbV3ScoresTeamProfilesAll` · `cbbV3ScoresTeamSeasonStats` · `cbbV3ScoresTeamsBasicAll` · `cbbV3ScoresTournamentHierarchy` |
| `client.cbbV3Odds` | `CbbV3Odds` | 34 | `cbbV3OddsAlternateMarketPreGameOddsByDate` · `cbbV3OddsAlternateMarketPreGameOddsLineMovement` · `cbbV3OddsBettingEventsByDate` · `cbbV3OddsBettingEventsBySeason` · `cbbV3OddsBettingFuturesBySeason` · `cbbV3OddsBettingFuturesBySeasonSportsbookGroup` · `cbbV3OddsBettingMarket` · `cbbV3OddsBettingMarketSportsbookGroup` · `cbbV3OddsBettingMarketsByEvent` · `cbbV3OddsBettingMarketsByEventSportsbookGroup` · `cbbV3OddsBettingMarketsByGame` · `cbbV3OddsBettingMarketsByGameSportsbookGroup` · `cbbV3OddsBettingMarketsByMarketType` · `cbbV3OddsBettingMarketsByMarketTypeSportsbookGroup` · `cbbV3OddsBettingMetadata` · `cbbV3OddsBettingPlayerPropsByGame` · `cbbV3OddsBettingPlayerPropsByGameSportsbookGroup` · `cbbV3OddsBettingResultsByMarket` · `cbbV3OddsBettingResultsByMarketSportsbookGroup` · `cbbV3OddsBettingSplitsByBettingMarket` · `cbbV3OddsBettingSplitsByGame` · `cbbV3OddsBettingTrendsByMatchup` · `cbbV3OddsBettingTrendsByTeam` · `cbbV3OddsInGameOddsByDate` · `cbbV3OddsInGameOddsByDateSportsbookGroup` · `cbbV3OddsInGameOddsLineMovement` · `cbbV3OddsInGameOddsLineMovementSportsbookGroup` · `cbbV3OddsInGameOddsLineMovementWithResultingSportsbookGroup` · `cbbV3OddsPreGameAndPeriodGameOddsByDateSportsbookGroup` · `cbbV3OddsPreGameAndPeriodGameOddsLineMovementSportsbookGroup` · `cbbV3OddsPreGameAndPeriodGameOddsLineMovementWithResultingSportsbookGroup` · `cbbV3OddsPreGameOddsByDate` · `cbbV3OddsPreGameOddsLineMovement` · `cbbV3OddsSportsbooksByActive` |
| `client.cbbV3Stats` | `CbbV3Stats` | 11 | `cbbV3StatsBoxScoreFinal` · `cbbV3StatsBoxScoreLiveFinal` · `cbbV3StatsBoxScoresByDateFinal` · `cbbV3StatsBoxScoresByDateLiveFinal` · `cbbV3StatsBoxScoresDeltaByDate` · `cbbV3StatsPlayerGameLogsBySeason` · `cbbV3StatsPlayerGameStatsByDateFinal` · `cbbV3StatsPlayerGameStatsByDateLiveFinal` · `cbbV3StatsPlayerSeasonStats` · `cbbV3StatsPlayerSeasonStatsByTeam` · `cbbV3StatsTeamGameStatsByDateFinal` |
| `client.wnbaV3Scores` | `WnbaV3Scores` | 40 | `wnbaV3ScoresAreGamesInProgress` · `wnbaV3ScoresBettingEventsByDate` · `wnbaV3ScoresBettingEventsBySeason` · `wnbaV3ScoresBettingFuturesBySeason` · `wnbaV3ScoresBettingFuturesBySeasonSportsbookGroup` · `wnbaV3ScoresBettingMarket` · `wnbaV3ScoresBettingMarketSportsbookGroup` · `wnbaV3ScoresBettingMarketsByEvent` · `wnbaV3ScoresBettingMarketsByEventSportsbookGroup` · `wnbaV3ScoresBettingMarketsByGame` · `wnbaV3ScoresBettingMarketsByGameSportsbookGroup` · `wnbaV3ScoresBettingMarketsByMarketType` · `wnbaV3ScoresBettingMarketsByMarketTypeSportsbookGroup` · `wnbaV3ScoresBettingMetadata` · `wnbaV3ScoresBettingPlayerPropsByGame` · `wnbaV3ScoresBettingPlayerPropsByGameSportsbookGroup` · `wnbaV3ScoresBoxScoreFinal` · `wnbaV3ScoresBoxScoreLiveFinal` · `wnbaV3ScoresBoxScoresByDateFinal` · `wnbaV3ScoresBoxScoresByDateLiveFinal` · `wnbaV3ScoresGamesByDateFinal` · `wnbaV3ScoresGamesByDateLiveFinal` · `wnbaV3ScoresGamesBasicByDateFinal` · `wnbaV3ScoresGamesBasicByDateLiveFinal` · `wnbaV3ScoresPlayerDetailsByActive` · `wnbaV3ScoresPlayerDetailsByTeam` · `wnbaV3ScoresPlayerSeasonStats` · `wnbaV3ScoresPlayerSeasonStatsByTeam` · `wnbaV3ScoresPreGameOddsByDate` · `wnbaV3ScoresPreGameOddsLineMovement` · `wnbaV3ScoresSchedules` · `wnbaV3ScoresSchedulesBasic` · `wnbaV3ScoresSeasonCurrent` · `wnbaV3ScoresSportsbooksByActive` · `wnbaV3ScoresStadiums` · `wnbaV3ScoresStandings` · `wnbaV3ScoresTeamGameStatsByDateFinal` · `wnbaV3ScoresTeamGameStatsByDateLiveFinal` · `wnbaV3ScoresTeamProfilesAll` · `wnbaV3ScoresTeamSeasonStats` |
| `client.cwbbV3Scores` | `CwbbV3Scores` | 8 | `cwbbV3ScoresAreAnyGamesInProgress` · `cwbbV3ScoresGamesByDateFinal` · `cwbbV3ScoresGamesByDateLiveFinal` · `cwbbV3ScoresLeagueHierarchy` · `cwbbV3ScoresSchedules` · `cwbbV3ScoresSchedulesBasic` · `cwbbV3ScoresSeasonCurrent` · `cwbbV3ScoresTeamProfilesAll` |
| `client.soccerV4Scores` | `SoccerV4Scores` | 21 | `soccerV4ScoresAreasCountries` · `soccerV4ScoresCompetitionsLeagues` · `soccerV4ScoresGamesByCompetitionLiveFinal` · `soccerV4ScoresGamesByDateFinal` · `soccerV4ScoresGamesByDateLiveFinal` · `soccerV4ScoresGamesBasicByDateFinal` · `soccerV4ScoresGamesBasicByDateLiveFinal` · `soccerV4ScoresMembershipsByActive` · `soccerV4ScoresMembershipsByCanceled` · `soccerV4ScoresMembershipsByRecentlyChanged` · `soccerV4ScoresMembershipsHistoricalByTeam` · `soccerV4ScoresPlayerDetailsByTeam` · `soccerV4ScoresPlayerProfilesByTeam` · `soccerV4ScoresSchedules` · `soccerV4ScoresSchedulesBasic` · `soccerV4ScoresStandings` · `soccerV4ScoresTeamGameStatsByDateLiveFinal` · `soccerV4ScoresTeamProfilesByCompetition` · `soccerV4ScoresTeamProfilesBySeason` · `soccerV4ScoresTeamSeasonStats` · `soccerV4ScoresVenues` |
| `client.soccerV4Headshots` | `SoccerV4Headshots` | 1 | `soccerV4HeadshotsHeadshots` |
| `client.soccerV4Projections` | `SoccerV4Projections` | 4 | `soccerV4ProjectionsDfsSlatesByDate` · `soccerV4ProjectionsPlayerDetailsByInjured` · `soccerV4ProjectionsProjectedPlayerGameStatsByDate` · `soccerV4ProjectionsUpcomingDfsSlatesByCompetition` |
| `client.soccerV4Stats` | `SoccerV4Stats` | 11 | `soccerV4StatsBoxScoreFinal` · `soccerV4StatsBoxScoreLiveFinal` · `soccerV4StatsBoxScoresByDateFinal` · `soccerV4StatsBoxScoresByDateLiveFinal` · `soccerV4StatsBoxScoresDeltaByDate` · `soccerV4StatsFantasyPointsByDate` · `soccerV4StatsLineupsByDate` · `soccerV4StatsPlayerGameStatsByDateFinal` · `soccerV4StatsPlayerGameStatsByDateLiveFinal` · `soccerV4StatsPlayerSeasonStats` · `soccerV4StatsTeamGameStatsByDateFinal` |
| `client.soccerV4Odds` | `SoccerV4Odds` | 28 | `soccerV4OddsBettingEventsByDate` · `soccerV4OddsBettingEventsBySeason` · `soccerV4OddsBettingFuturesBySeason` · `soccerV4OddsBettingFuturesBySeasonSportsbookGroup` · `soccerV4OddsBettingMarket` · `soccerV4OddsBettingMarketSportsbookGroup` · `soccerV4OddsBettingMarketsByEvent` · `soccerV4OddsBettingMarketsByEventSportsbookGroup` · `soccerV4OddsBettingMarketsByGame` · `soccerV4OddsBettingMarketsByGameSportsbookGroup` · `soccerV4OddsBettingMarketsByMarketType` · `soccerV4OddsBettingMarketsByMarketTypeSportsbookGroup` · `soccerV4OddsBettingMetadata` · `soccerV4OddsBettingPlayerPropsByGame` · `soccerV4OddsBettingPlayerPropsByGameSportsbookGroup` · `soccerV4OddsInGameOddsByDate` · `soccerV4OddsInGameOddsByDateSportsbookGroup` · `soccerV4OddsInGameOddsLineMovement` · `soccerV4OddsInGameOddsLineMovementSportsbookGroup` · `soccerV4OddsInGameOddsLineMovementWithResultingSportsbookGroup` · `soccerV4OddsPeriodGameOddsByDate` · `soccerV4OddsPeriodGameOddsLineMovement` · `soccerV4OddsPreGameAndPeriodGameOddsByDateSportsbookGroup` · `soccerV4OddsPreGameAndPeriodGameOddsLineMovementSportsbookGroup` · `soccerV4OddsPreGameAndPeriodGameOddsLineMovementWithResultingSportsbookGroup` · `soccerV4OddsPreGameOddsByDate` · `soccerV4OddsPreGameOddsLineMovement` · `soccerV4OddsSportsbooksByActive` |
| `client.golfV2` | `GolfV2` | 23 | `golfV2CoursesCourses` · `golfV2CurrentseasonSeasonCurrent` · `golfV2DfsslateownershipprojectionsbyslateidDfsSlateOwnershipProjectionsBySlate` · `golfV2DfsslatesbytournamentDfsSlatesByTournament` · `golfV2FantasygamestatsbytournamentFantasyPointsByTournament` · `golfV2LeaderboardLeaderboardLiveFinal` · `golfV2LeaderboardbasicLeaderboardBasic` · `golfV2LeaderboardbasicfinalLeaderboardBasicFinal` · `golfV2LeaderboardfinalLeaderboardFinal` · `golfV2NewsNews` · `golfV2NewsbydateNewsByDate` · `golfV2PlayerPlayer` · `golfV2PlayersPlayerProfilesAll` · `golfV2PlayerseasonstatsPlayerSeasonStats` · `golfV2PlayertournamentholescoresPlayerHoleScoresByTournamentLiveFinal` · `golfV2PlayertournamentholescoresfinalPlayerHoleScoresByTournamentFinal` · `golfV2PlayertournamentprojectionstatsPlayerProjectedStatsByTournament` · `golfV2PlayertournamentroundscoresScoresByTournamentLiveFinal` · `golfV2PlayertournamentroundscoresfinalScoresByTournamentFinal` · `golfV2RankingsRankings` · `golfV2TournamentsScheduleBySeason` · `golfV2TournamentsSchedules` · `golfV2UpcomingdfsslateownershipprojectionsDfsSlateOwnershipProjectionsUpcoming` |
| `client.golfV3Headshots` | `GolfV3Headshots` | 1 | `golfV3HeadshotsHeadshots` |
| `client.golfV3RotoBallerPremiumNews` | `GolfV3RotoBallerPremiumNews` | 2 | `golfV3NewsRotoballerPremiumNews` · `golfV3NewsRotoballerPremiumNewsByDate` |
| `client.golfV3Odds` | `GolfV3Odds` | 21 | `golfV3OddsBettingEventsByDate` · `golfV3OddsBettingEventsBySeason` · `golfV3OddsBettingMarket` · `golfV3OddsBettingMarketSportsbookGroup` · `golfV3OddsBettingMarketsByEvent` · `golfV3OddsBettingMarketsByEventSportsbookGroup` · `golfV3OddsBettingMarketsByMarketType` · `golfV3OddsBettingMarketsByMarketTypeSportsbookGroup` · `golfV3OddsBettingMarketsByTournament` · `golfV3OddsBettingMarketsByTournamentSportsbookGroup` · `golfV3OddsBettingMetadata` · `golfV3OddsBettingPlayerPropsByTournamentSportsbookGroup` · `golfV3OddsInPlayTournamentOdds` · `golfV3OddsInPlayTournamentOddsSportsbookGroup` · `golfV3OddsInPlayTournamentOddsLineMovement` · `golfV3OddsInPlayTournamentOddsLineMovementSportsbookGroup` · `golfV3OddsSportsbooksByActive` · `golfV3OddsTournamentOdds` · `golfV3OddsTournamentOddsSportsbookGroup` · `golfV3OddsTournamentOddsLineMovement` · `golfV3OddsTournamentOddsLineMovementSportsbookGroup` |
| `client.nascarV2` | `NascarV2` | 13 | `nascarV2DriverDriverProfilesByDriver` · `nascarV2DriverraceprojectionsProjectedPlayerRaceStatsByRace` · `nascarV2DriversDriverProfilesAll` · `nascarV2DriversbyactiveDriversByActive` · `nascarV2EntrylistbyraceEntryListByRace` · `nascarV2FantasygamestatsbyraceFantasyPointsByRace` · `nascarV2LeaderboardbasicLeaderboardBasic` · `nascarV2LeaderboardbasicfinalLeaderboardBasicFinal` · `nascarV2RaceresultLeaderboardLiveFinal` · `nascarV2RaceresultfinalLeaderboardFinal` · `nascarV2RacesSchedules` · `nascarV2SeriesSeries` · `nascarV2TracksTracks` |
| `client.nascarV3Odds` | `NascarV3Odds` | 10 | `nascarV3OddsBettingEventsByDate` · `nascarV3OddsBettingEventsBySeason` · `nascarV3OddsBettingMarket` · `nascarV3OddsBettingMarketsByEvent` · `nascarV3OddsBettingMarketsByMarketType` · `nascarV3OddsBettingMarketsByRace` · `nascarV3OddsBettingMetadata` · `nascarV3OddsRaceOdds` · `nascarV3OddsRaceOddsLineMovement` · `nascarV3OddsSportsbooksByActive` |
| `client.mmaV3Scores` | `MmaV3Scores` | 6 | `mmaV3ScoresEvent` · `mmaV3ScoresFighterProfileByFighter` · `mmaV3ScoresFighterProfilesAll` · `mmaV3ScoresFighterProfilesBasicAll` · `mmaV3ScoresLeagues` · `mmaV3ScoresSchedules` |
| `client.mmaV3Odds` | `MmaV3Odds` | 10 | `mmaV3OddsBettingEventsByEvent` · `mmaV3OddsBettingEventsUpcoming` · `mmaV3OddsBettingFighterPropsByEvent` · `mmaV3OddsBettingMarket` · `mmaV3OddsBettingMarketsByEvent` · `mmaV3OddsBettingMarketsByMarketType` · `mmaV3OddsBettingMetadata` · `mmaV3OddsEventFightOdds` · `mmaV3OddsEventFightOddsLineMovement` · `mmaV3OddsSportsbooksByActive` |
| `client.mmaV3Stats` | `MmaV3Stats` | 4 | `mmaV3StatsFightBasicFinal` · `mmaV3StatsFightBasicLiveFinal` · `mmaV3StatsFightFinal` · `mmaV3StatsFightLiveFinal` |

Every operation has the same call shape — `op(request, options?)`, one **flat, channel-blind** request object first and `RequestOptions` (`{ signal }`) second — and returns `ApiPromise<T, E>`.

⚠ **The request type name is not uniformly `<Operation>Request`.** A name that collides inside its namespace is promoted to `<Operation>RequestParams` instead — no operation in this SDK takes that spelling at this version. Take the name from the operation's **Signature** bullet on its map page; never construct it from the method name.

## SDK map — look up first, open the file second

The SDK ships a generated map, and `package.json`'s `files` list includes it, so **installing the package gives you the map** — no clone is needed. It sits at the package root, the directory holding `package.json` and the `src/` tree:

- **`sdk-map.md`** — the index: client construction with the full `ClientOptions` table, the *Not on this SDK* table, the two error families with `ApiResult` and `.asApiResult()`, wire serialization for every channel, **the full enum table with every member and its wire value**, servers and auth, runtime and packaging, and the link table into the operations pages.
- **`map/operations/<resource>.md`** — one page per resource, one `###` block per operation, with bullets in the fixed order **Server**, **Signature**, **Wire** (verb and route), **Auth**, **Request body**, **SDK-sent**, **Returns**, **Error**, **Error arms** — then a **Fields** table giving every request field its channel, wire name, type, required flag and default, and a **Type sources** table naming the declaring file and schema value of every type the operation mentions.

Locate the installed package before you rely on a lookup:

```bash
node -e "console.log(require.resolve('sports-data-io/package.json'))"
```

Failing that it is at `node_modules/sports-data-io/`. **If the package is not installed, there is no map and no source to read** — mark the fact `UNVERIFIED` and say what would settle it rather than answering from memory.

Every `Source` path on the map is relative to that package root, so `src/models/<file>.ts` opens as written from there — the package ships its `src/` tree, so the path resolves inside `node_modules/sports-data-io/` exactly as the map writes it. An import specifier ending `.js` inside that source is the NodeNext spelling of the sibling `.ts` file.

**The map is the locator; the source files are the shapes.** Read the map first — signatures, routes, request fields with their channels and defaults, return types, error arms, enum values, and which file declares a type are all answered there without opening a single `.ts` file. Then open the one file the map names for what it deliberately does not carry: a model's members, whether each is required, optional or nullable. The map says so itself — *"Shapes live only in the source … Do not derive the path from the type name."*

**`sdk-map.md` carries the invariants every operation block assumes, so read it before any `map/operations/` page**; the pages are written to be read beside it. And **silence means the default**: the index states what holds for every operation — the call shape, the flat channel-blind request object, the `ApiPromise<T, E>` return, the default server group, no pagination and no streaming — and a block departs from one only by saying so. Take the default and move on rather than opening the source to confirm it.

**The map carries shapes; what an operation *means* lives elsewhere.** When *what* to pass depends on meaning — which values a field accepts beyond its type, a rule that couples two fields, what a defaulted header actually selects — the map will not settle it. Read that operation's entry in `api-reference.md` at the package root, keyed by the same signature, *before* writing the sheet row, and record what you found. A value you already "know" for a field the map types as a plain `string` is a lookup, not a recall — the memory ban applies to it.

## Contract facts — the map first, then the source file

**Seven of these are map lookups — don't open a source file for them:** an operation's signature; its request fields with channel, wire name, required flag and default; its return type; its error subclass and the arms with the status each covers; the `ClientOptions` fields and their defaults; the environments, base URLs and auth wiring; **and every enum's members with their wire values**, which `sdk-map.md` tabulates in full.

The table below covers everything else, and the full body behind a map row. Paths are relative to `node_modules/sports-data-io/`:

| Question | File |
| --- | --- |
| A model's members, required (`f: T`) vs optional (`f?: T`) vs required-nullable (`f: T \| null`) | `src/models/<file the Type sources table names>.ts` |
| The operation method body and the request it builds | `src/resources/<resource>.ts` |
| The per-operation request and error types (merged namespace) | the `export namespace <Resource>` block at the foot of the same file |
| Client construction, resource getters | `src/client.ts` |
| `ClientOptions` fields and `DEFAULT_CLIENT_OPTIONS` | `src/client-options.ts` |
| Environments, base URLs, override merging | `src/servers.ts` |
| Auth scheme wiring, token endpoint, credential placement | `src/auth-schemes.ts`, `src/core/auth/credentials.ts`, `src/core/auth/oauth2-strategies.ts` |
| The transport: timeout clamp, `fetch` resolution, 401 invalidation, 2xx-vs-error split | `src/core/raw-client.ts` |
| Error classes and `ErrorKind` | `src/core/errors.ts`, `src/core/response-error.ts` |
| `ApiPromise`, `ApiResult`, `.asApiResult()`, the `Symbol.species` behaviour | `src/core/api-promise.ts` |
| `RequestOptions` (it is `{ signal }` and nothing else) | `src/core/api-request.ts` |
| Schema decode/encode, `SchemaError`, `Encoded<T>` | `src/core/validation/schema-error.ts` and its directory |
| Wire serialization per channel | `src/core/param-value.ts`, `src/core/url.ts`, `src/core/headers.ts`, `src/core/params.ts` |
| What an operation *means* — field semantics, coupling rules | `api-reference.md` at the package root |

**Read scoped.** Search for the one symbol and read the lines around it rather than whole files, and never copy a design comment's rationale onto a contract sheet — the sheet carries facts an implementer must obey, not the reasoning behind them.

Keep lookups cheap — the rules that keep a session's context small:

- Collect the contracts for **every** in-scope operation in **one** pass — signature, request fields with channels and defaults, required members, the error arms, enum values — into a short **contract sheet** in your plan, then implement from the sheet. Don't re-open a map page per field, and never re-look-up a fact the sheet already carries.
- Recurse into a model's members only where the task actually sets them — a full transitive expansion is hundreds of rows nobody needs.
- **Never grep, glob or `find` the package to *locate* a type** — the map is the locator, and it says so. Grep only *inside* the file its **Type sources** table names, for the symbol. A sweep for a cross-cutting *shape* is a different question and is fine: "every field typed `unknown`", "every required-nullable member" are things nothing indexes, and one targeted `grep -rn` over `src/models/` is the right tool — record what it found on the sheet.
- Trust the compiler over this page: if a name here ever fails to type-check, re-read the file the table above names and report the drift; never patch around it from memory.

## Integration workflow — load the companion skill at each step

Before you write the code for each step, load the named companion skill — even if you have already read the relevant file. Each step calls out the trap the signature hides (in *parens*). A typical integration reaches them in this order:

1. **Client construction & lifetime** — load **typescript-client-initialization** before you write `new SportsDataIoClient(…)`. (*The signature won't tell you:* every option is optional, so a client built with no arguments compiles and talks to the default environment with no credential; the client must be **long-lived and app-scoped**, never rebuilt per request, because the resource getters live on it; there is no `close()` or `dispose()` — it owns no pool, only a `fetch`; and when no `fetch` is reachable the **constructor** throws `SdkError`, not the first call.)
2. **Authentication** — load **typescript-authentication** before you set credentials. The 2 schemes are `apiKeyHeader`, `apiKeyQuery` on `ClientOptions`. (*The signature won't tell you:* the field is optional — omit it and every request goes out unauthenticated with no failure at construction; and a 401 invalidates the cache without retrying the current call. Load secrets from the environment or a secret store, never hardcode.)
3. **Calling an endpoint** — load **typescript-calling-endpoints** before the first `client.<resource>.<operation>(…)` call. (*The signature won't tell you:* the request object is **flat and channel-blind** — a field named `body` *is* the whole request body and every other field is fanned out to path, query or header by the SDK, so nothing is nested by channel; **an omitted field that has a default is still sent, with that default**; **One operation resolves to `undefined` (`wnbaV3Scores.wnbaV3ScoresStandings`)**; and `.asApiResult()` must be called on the value the operation returned, because `ApiPromise` overrides `Symbol.species` and `.then()`/`.catch()` hand back a plain `Promise` with the method gone.)
4. **Models** — load **typescript-models** the moment a request/response member is not a plain string or number. (*The signature won't tell you:* models are plain `type`s built from object literals — no constructor, no builder; `f?: T` means omit the key, while `f: T | null` is **required and nullable** and `null` is a distinct value; enums are **open** (`const` companion plus a union admitting `(string & {})`), so the schema validates the base type only and an unknown server value round-trips instead of throwing — use `.values` to test membership yourself; and every type has a schema companion usable in both directions.)
5. **Error handling** — load **typescript-error-handling** before you write any `try/catch`. (*The signature won't tell you:* there are **two disjoint families** — `ResponseError` and its per-operation subclasses for an API error status, and the `SportsDataIoError` set (`ConnectionError`, `TimeoutError`, `AbortError`, `SdkError`, `SchemaError`, `AuthError`) for no usable response — and neither is `instanceof` the other, so a complete catch needs both arms; **arm tags are schema-derived, not statuses** (see the sheet checklist below); a malformed 2xx body rejects with `SchemaError`, not `ResponseError`, and `.asApiResult()` does not convert it; and a missing response field the schema permits is silently `undefined` rather than any error at all.)
6. **Configuration & resilience** — load **typescript-configuration-resilience** when you set the base URL, timeouts, proxies, TLS, or logging. (*The signature won't tell you:* **the SDK performs no retries at all** — a failed call rejects once, so retry/backoff is entirely yours to build or deliberately omit; **there is no logging and there are no hooks, middleware or interceptors** — `ClientOptions.fetch` is the single extension point for all of it; `timeout` is client-wide with **no per-request timeout**, and a non-finite or non-positive value is not "no timeout" but a fallback to the transport's own ceiling; and a `fetch` replacement that drops `init.signal` makes both the timeout and every `RequestOptions.signal` inert.)
7. **Testing** — load **typescript-testing** before you stub the SDK. (*The signature won't tell you:* the seam is **`ClientOptions.fetch`**, not the client class and not the resource classes — whose constructors take unexported engine internals, so they cannot be instantiated in a test; stub bodies in **wire shape** and let the SDK decode them; assert on the request the SDK actually built, headers included; and cover the failure kinds a `ResponseError`-only test misses, `SchemaError` above all.)

## What a contract sheet must carry for this SDK

Beyond the usual signatures and model members, a contract sheet for the SportsDataIO TypeScript SDK is incomplete without these, because each one is a decision the implementer cannot make correctly from the signature alone.

1. **Which host each deployment talks to**, and where that is set. This SDK declares one environment, `ServerEnvironment.Production`, so any other host is a `serverOptions` base-URL override rather than an environment member — give the override path on the sheet.
2. **One operation resolves to `undefined` (`wnbaV3Scores.wnbaV3ScoresStandings`)** — `await` gives you nothing to inspect, so **`.asApiResult()` is the only way to observe their status and headers** — decide the mode at write time, not by retrofit.
3. **The exact request type name per operation**, taken from the **Signature** bullet.
4. **Every request field with its channel, wire name and default**, because the request object is flat and channel-blind and the SDK fans fields out. An omitted field that has a default is still sent with that default, so a defaulted header shapes the response whether or not the sheet mentions it. Any caller-supplied idempotency or request-id field is the ONLY idempotency this SDK has: it injects none and `RequestOptions` is `{ signal }` only.
5. **Required vs optional vs required-nullable** for every model member the task sets — `f: T` required, `f?: T` omit the key, `f: T | null` required and nullable. And that under `exactOptionalPropertyTypes` an absent optional is **omitted or spread**, never assigned `undefined`.
6. **The error arms for each operation in scope, with the status each covers — and the warning that arm tags are schema-derived, not status codes.** Every operation rejects with its own `ResponseError` subclass narrowed on `err.payload.kind`, and a tag comes from the arm's **body schema**: an arm whose body is a direct model reference is named after that model in lower camel (`"apiError"`), and every other body — a primitive, an array, a map, or no content — is named `"error{Status}"` (`"error400"`, `"error4XX"`, `"errorDefault"`), with a numeric suffix on the second of two arms that would otherwise land on the same name. The same tag means different statuses on different operations, and the same status carries different tags — so a tag is only meaningful beside the arm table it came from, and a shared helper that switches on `kind` across operations is a bug. 741 of 741 operations declare typed error bodies; the rest reject with the base `ResponseError`. Every operation also carries an always-present `"undeclared"` arm holding `rawBody: ArrayBuffer`, for which **matcher precedence** matters: an exact numeric status is looked up across the whole table first, and only then does the first covering wildcard or range win.
7. **That a malformed or drifted 2xx body rejects with `SchemaError`, not `ResponseError`, in both response modes** — `.asApiResult()` converts an HTTP error status, never a Family B failure. Any sheet row for a call whose result is used must name the members the implementer has to assert on, because a thin or truncated body decodes without complaint and the hole surfaces later.
8. **That the SDK performs no retries, no logging, no pagination and no streaming at all**, and that `ClientOptions.fetch` is the one seam where any of it can be added — so whatever the task needs there is yours to build or deliberately omit. Say which.
9. **That `Event` imported from this package is a model type, not the global of that name** — every sheet that references one should carry the alias it will be imported under. The error base is re-exported as `SportsDataIoError` for the same reason.
10. A **REQUIRED READING** block naming the `typescript-*` companions that govern the steps, with inline `MUST load` pointers.

